#pragma once

// This header used to contain a full Character definition but a duplicate copy
// exists under the cartesian path. To avoid duplicate type definitions when
// both include styles are used across the codebase we forward all includes to
// the canonical cartesian Character header.

#include <client/game/entities/cartesian/characters/character/Character.h>
